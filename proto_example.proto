syntax = "proto3";

package proto_example;

message Request {
    string message = 1;
}

message Response {
    string message = 1;
}

service GreetingService {
    rpc SayHello(Request) returns (Response) {}
}
